"use strict";
// Ensure npm pack only ships the JS wrapper, not bench artifacts or native caches.
const fs = require("fs");
const path = require("path");
console.log("[tropical-bundler] prepack ok");
